<?php

/** $_POST
 * A variáveis pré-definida $_POST possui um array associativo
 * contendo todas as entradas passadas para o script via
 * método POST do protocolo HTTP quando utilizado como valor
 * do cabeçalho HTTP Content-Type:
 *      - application/x-www-form-urlencoded
 *      - multipart/form-data
 */

 var_dump($_POST);